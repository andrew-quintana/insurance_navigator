[pytest]
testpaths = tests/environment
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Add markers for different environments
markers =
    development: marks tests that check development environment
    staging: marks tests that check staging environment
    production: marks tests that check production environment

# Don't warn about markers
addopts = --strict-markers 